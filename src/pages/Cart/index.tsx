import React from 'react';
import {SafeAreaView} from 'react-native';
import {Center, Text} from 'native-base';

export function CartPage() {
  return (
    <SafeAreaView>
      <Center>
        <Text fontSize="xl">CartPage</Text>
      </Center>
    </SafeAreaView>
  );
}
